//immediately inovoked function expressions(IIFE)

// to prevent global scope pollution iife is used 

// (define a function)(to call the function)

(function biryani(){
    console.log('connected');
})();//semi colan pettadam marchpote end avvadu 

(  (name) => {
    console.log(`${name}`)
}  )('mohan')